# Paddock — ideas ledger

This is the single source of truth for everything we might build. Triaged at the end of every session.

**Rules:**
1. Every new idea forces you to pick one to drop or park. There is no infinite capacity.
2. Inbox is append-only during a session. Triage at session end only — never mid-task.
3. Sections cap: Now ≤ 3, Next ≤ 5. Inbox uncapped. Parked + Killed unbounded — they're the ledger.
4. Inbox entries are one sentence, no formatting. Triaged entries get a one-line "why" appended.

Time-based scheduling lives in `SCHEDULE.md`.

---

## Now (≤ 3, in flight)

_Triage 2026-06-11 (operator 15-item batch organized into waves W1–W8; sequencing + details in `SCHEDULE.md` backlog stubs). Retired from the old list: Fotis Supabase sit-down + provisioning → coupled to W7 in the Inbox (the blog/UGC ask IS the S9 trigger); ADAC champions → done (0.24.1 curated 2026 winners); B-content → superseded (histories ×15 shipped 0.20.0, Rules retired 0.19.0; blog posts fold into W7/W8)._

1. **Betting / Social game — LIVE, in refinement.** Live end-to-end since 0.46.0 (F1 winner/podium/top-10 markets on weekend pages, monthly credits, friend leagues, P4 prizes, the `/social` area). Refinement queue, in priority order: **(a) landing-page marketing** for the betting/social game (your named follow-on — honest no-cashout framing); **(b) richer league leaderboard** (net credits / streak / last-N / honours / colour dot — design pass first); **(c) real-odds API adapter** (real bookmaker odds for the F1 winner via the RapidAPI gateway, clamped through the house band; model stays fallback for podium/top10/exact — needs provider + key + name-matching + datacenter verify); **(d) `exact_position` go-live** (engine + UI built + held — interaction-verify the picker signed-in, then add to `MARKET_BUILDERS`); **(e) forecast market** (pick multiple drivers + their exact finishing positions, operator 2026-06-23) — **→ SHIPPED 0.82.0 (#225), DORMANT** (built + locally-verified + prod-migrated; payout = least(product, 500); NOT in `MARKET_BUILDERS` — go-live = interaction-verify the picker signed-in + add `createForecastMarket` to the builders). Also shipped this session: **(d) `exact_position`** stays held; per-league **bet limits 0.81.0 (#224)** + **direct friend-invite 0.77.0 (#217)**. _(W1 weekend overhaul retired from Now — W1a/b/c + the 0.61.0 weekend tabs all shipped. **2026-06-23 batch shipped: account accordions 0.62.0, play round-bars 0.63.0, friend-links 0.64.0, calendar M/W/D 0.65.0, home-customise phase-1 0.66.0, invite-join Safari fix 0.61.2.**)_ _**2026-06-24 parallel batch (4 PRs #229–#232) SHIPPED:** (a) landing marketing + (b) richer leaderboard + betting reminders/results-in notifs (0.86.0); home lazy-load perf + customise-on-its-own-page + widget-discovery gallery + Social umbrella (Play folded into Social) (0.85.0); threads per-series tags (0.87.0); F1 Jolpica-521-outage resilience (0.84.0). Still queued: **(c) real-odds adapter** (operator: keep last); **(d) exact_position + (e) forecast go-live** = interaction-verify the signed-in picker → add to `MARKET_BUILDERS` (this is the operator's "can't select more than one driver for podium/points" report — the multi-driver market is forecast, built + dormant)._ **Triage 2026-07-03: (d) exact_position is LIVE since 0.95.0 (in `MARKET_BUILDERS`) — closed, was stale here. (e) forecast LIVE 0.88.0 — remaining watch: operator checks the first real settlement (Austria) on prod. Still genuinely open in this slot: (c) real-odds adapter (operator provider+key) only; multi-series markets + dormant grid market building in the 2026-07-03 wave.**
2. ✅ **Security audit — DONE (was stale here).** Ran 2026-06-11 (`docs/research/security-audit-2026-06-11.md`), re-verified 2026-06-21 incl. the contact rate-limit; follow-on hardening shipped (0.139.4 SSRF allowlist, CSP Report-Only). W8 launch gate SATISFIED. Slot open — residuals tracked separately: CSP→enforcing flip, Sentry (DSN-gated). _(Triage 2026-07-03.)_
3. **B-perf execution.** Carried since 2026-05-20 — top non-feature debt. Baselines in `docs/perf-baselines.md`; levers: Clerk lazy, 3rd-party deferral, CSS critical path. Re-baseline first — the redesign changed every page since capture.

## Next (≤ 5, queued — start within ~1–2 weeks)

1. **W5 — per-page layout spec, desktop + phone.** One design session documented in the redesign doc; feeds W1/W4. Operator: "plan out how the layout of each page will be like on desktop and then do the same for on phone."
2. ✅ **W3 — DONE (was stale here).** Rules essentials ×15 shipped INTO About 0.31.0 (2026-06-11); 15/15 `content/series/*/rules.md` verified on disk. Launch gate SATISFIED. Residual (not W3): `overview.md` is a TODO stub for 13/15 series. _(Triage 2026-07-03.)_
3. **W4 — driver + team profile pages.** Photos/bios/stats, multi-session. Verify drivers.json coverage first (gap was 13 series in May — recheck before scoping). Absorbs the old S6 enrichment entry.
4. **W8 — v1.0 launch program.** SCOPE LOCKED (operator 2026-06-11): W1 + security audit + W3 + W4 gate the launch. **Triage 2026-07-03: W1 ✅ (incl. point-in-time standings) · security ✅ · W3 ✅ — the ONLY remaining launch gate is W4 profiles.** Remaining W8 work: "out of early access" banner, marketing channel plan (IG/FB/Reddit/X/YouTube), launch checklist.
5. **Weather + news coverage audit (15 series).** Carried. Open-Meteo (venue-local date) + news feed per next-weekend of every series; output = gap list + curation pass.

## Inbox (unfiltered, append-only)

- 2026-07-09 Add a tracks map — a world map pinpointing all 138 circuit venues with a search bar, in the /information tracks area (leverages the enriched track data + coordsVerified coordinates).
- 2026-07-09 Page-width fix — /news, /information (Answers) and /social render in a narrow centred column with large empty side margins; port the full-device-width container that /calendar, /series and home already use to these pages.
- 2026-07-09 AI-assistant bubble polish — make the Race Engineer chat launcher larger on bigger screens and swap the headset icon for a race-engineer/person figure (preferred) or a wrench.
- 2026-07-09 Enrich the 43 thin INDEXED /information pages found in the hub-wide audit — ~36 generated champions Q&A (who-won / most-X / most-successful-team) + 7 sparse per-country track-list pages — via a generated.ts change (add runner-up/team/margin/history context), or de-index them.
- 2026-07-08 Wire the AI assistant (Race Engineer chat widget) to the `/information` hub content — the editorial answers, verified track circuit guides, team histories and champion Q&A — so it can answer motorsport questions from our own fact-checked content (the assistant's Phase-2 grounded-data Q&A, now with the information hub as the retrieval source; guardrail: answer only from verified/indexed entries so it never surfaces an unreviewed draft).

- **2026-07-07 Motorsport Information hub `/information` — BUILT (overnight, branch `feat/motorsport-information-hub`, NOT pushed, proposed 0.178.0):** a "questions answered" + reference section — 577 pages (526 verified from our own champions data + 15 editorial explainers; 51 unverified drafts = 12 team histories + 38 tracks + 51-driver feeder rising-stars watchlist). Two-tier trust model gates indexing (verified+featured, cap 150) so only 51 hit the sitemap — the anti-spam answer to the operator's "careful with the sitemap" + the AdSense "low value" problem below. Feeds AdSense readiness (original content engine). Nav "Answers" menu + footer + ⌘K search. Design `docs/research/2026-07-07-information-hub.md`; 278-question backlog `docs/research/2026-07-07-information-question-catalog.json`. **Owed:** operator publish decision; fact-check + promote drafts; retry the broad tracks agent (stalled twice → seeded from `circuits.json`).

- **2026-07-07 AdSense "Low value content" rejection — DIAGNOSED (operator):** AdSense denies paddock-tracker.com ("your site isn't ready to show ads · policy violations · low value content"). Cause = Google's minimum-content / thin-content policy: the site is mostly AGGREGATED third-party data (standings/results/schedules from Jolpica/OpenF1/Wikipedia/motorsport.com) + Wikipedia-derived driver bios across many templated pages, with too little UNIQUE original content to "add value" over the sources. FIX (AdSense-readiness program): ship ORIGINAL editorial regularly (the blog cadence — previews/digests/analysis), complete the `overview.md` stubs (13/15 series are TODO placeholders), replace Wikipedia-copied bios with original write-ups, deepen thin templated pages — then re-request review. The blog cadence + writer role ARE the content engine. (Read the 4 Google policy pages before building.)

- **2026-07-07 AUDIT-FIRST strike (verified SHIPPED — stop resurfacing):** `/news` aggregated page (`app/(app)/news/` + top-level nav link), NASCAR trend-chart polish (all 4 flagged issues fixed in `SeasonTrendChart` — top-6 legend, `tickCount 8`, `(i)`/`(R)` stripped), and news-filter persistence (localStorage, both surfaces) are ALL already shipped. Only residual: a shareable-URL news filter (localStorage today, not a query param) — small, optional.

- **2026-07-07 Re-schedule a scheduled-but-unpublished post (operator):** once a post is approved/scheduled its publish time is fixed — `decidePost` only does draft→approved (no approved→approved re-schedule, no un-approve). Add a **re-schedule** action: edit `publish_at` while `status='approved'` and not yet published, for admins + the writer who owns it. Needs `reschedulePost(id, publishAt)` in `lib/blog.ts` (guard `status='approved'`) + wire into `POST /api/blog/[id]` (new action) with the same admin-or-owner authz as the writer role + a datetime control on the Scheduled rows in `PostModeration`. Emerged from the writer-role build.

- **2026-07-07 Weekly preview/digest blog cadence — PHASE 0 SHIPPED (#437, 0.176.5):** Thu-3pm preview + Mon-3pm digest for the marquee event of the week, as a scheduled Claude drafting routine (grounded numbers from our loaders + fact-checked research → `drafts/<slug>.md` → `draft-post.mts` → prod DB draft → operator approves/schedules in /blog → existing publish-posts cron ships at 15:00). Approval kept (no auto-publish). **Built + merged:** `scripts/weekend-post-context.mts` (marquee picker + grounded pack), `lib/blog-draft-md.ts` (.md→DraftInput, 5 tests), `draft-post.mts` (accepts `.md`/`--dry`), design doc + `docs/content-authoring/weekend-post-playbook.md` + local `/weekend-post` skill (gitignored); sample `drafts/motogp-german-grand-prix-2026-preview.md`. **TRIAGE → Next (near-term), not Now — blocked, not in flight:** P1 = run the insert with **prod Supabase creds** so drafts hit /blog (BLOCKED on the deferred Supabase issue — the SAME gate as the British GP queue + feeder intake; unblocking it clears all three); P2 = GH Actions headless `claude -p` trigger (model emits the `.md` → deterministic insert, never holds the service key; ScheduleWakeup rejected as session-bound). Doc: `docs/research/2026-07-07-blog-cadence-automation.md`.

- **2026-07-06 (later) W8 launch program KICKOFF SHIPPED (0.171.0, #420):** verified the f1-upgrades home widget signed-in (renders on `/app`, layout undisturbed) · shipped a **v1.0 launch-announcement banner that ships DARK** (`LaunchBanner` + `LAUNCH_ANNOUNCEMENT` flag in `lib/site.ts`, `active:false`; flip on launch day in the same commit that bumps to `1.0.0`) — visual-verified both states · wrote **`docs/launch-checklist.md`** (go/no-go gate + runbook + rollback) + **`docs/research/2026-07-06-launch-marketing.md`** (per-channel plan + first-post drafts + staggered week; nothing posted). **Finding worth keeping:** there is **no existing beta/early-access badge** in the app — "out of early access" is an announce surface, not a removal. **DECISIONS CAPTURED (operator, this session):** AI assistant = **account-gated** (free w/ account), model TBD, **build first after W8**; feeder intake = **tokened link** (no account) for v1; v1.0 = **build banner now (dark), flip on launch day**. Bet-display refinement was attempted + reverted — see its item below (needs a data-model decision).

- **2026-07-06 W4 + upgrades stretch SHIPPED (0.165.0→0.170.0, #401–#418) — stop resurfacing:** W4 team points chart → **all-constructors** view (#401/#405) · **F1 driver portraits 22/22** from Wikimedia Commons (#402/#404) · **cross-series driver-slug fix** — F1 owns `/drivers/max-verstappen`, ADAC → `-24h` (#404) · **per-weekend F1 UPGRADES** feature — FIA Car Presentation source, weekend section + **R1–R9 curated (251 parts)** + opt-in home widget (#415/#416/#418) · official highlights media R1–R9 / F2 R7 / F3 R5 (#406/#413) · calendar grid-stretch fix (#417). Team logos = keep the colour-bar (no free source). **AUDIT correction:** Champions + race-page collapsibles and historic team colours were ALREADY shipped; AppShell `--tint` is OBSOLETE (sidebar removed 0.17.0) — don't resurface. Full record: `docs/HANDOFF.md` 2026-07-06 block. **STILL OPEN:** home-upgrades widget needs a signed-in glance; the AI-assistant + feeder-intake **design docs** need your decisions before building.

- **AI assistant — "ask Paddock anything" (operator 2026-07-06, /feedback):** an in-app agent that knows motorsport + the website so a user can ask about a driver/series/rule or how to use the site and get instant help (product + design-doc first — retrieval over our own content, free-vs-account-gated, cost, and hard guardrails against hallucinated race data given the chart==standings accuracy bar). **Design doc: `docs/research/2026-07-06-ai-assistant.md`** (retrieval-grounded, refuse-when-uncovered, site-help MVP first, shares the global-search index, 5 open decisions). **→ MVP SHIPPED 0.172.0 (#422), ships DARK:** account-gated `/assistant`, Gemini Flash free (behind a swap seam), grounded in `content/assistant/site-help.md`, guardrailed + rate-limited (per-user 20/day + global 12/min, fail-closed). Decisions resolved (account-gated / Gemini Flash / answer-only / donor-escalation deferred). **Go-live = operator sets `GOOGLE_GENERATIVE_AI_API_KEY` + `ASSISTANT_MODEL` + a `/privacy` line, then verify a real answer on prod.** Phase 2 = grounded data Q&A. **→ UI REWORKED 0.173.0 (#422→#424):** operator disliked the page — now a floating **"Race Engineer" chat widget** (persistent launcher, conversational panel, multi-turn) replacing the `/assistant` page. Adds a 2nd go-live env var: **`NEXT_PUBLIC_ASSISTANT_ENABLED=1`** (shows the launcher; unset = ships dark, no launcher). Phase 2 also: streaming + conversation persistence. **→ LIVE + FULLY UPGRADED 2026-07-07 (0.174.0→0.176.2, #426–#431):** operator enabled Gemini **paid tier** (billing required for EEA/UK/CH users per Google's terms; free tier is denied + trains on data). Model `gemini-flash-lite-latest` (~€0.001/q). Shipped: links+bold rendering (#427), suggestion chips + conversation persistence + Contact escape-hatch (#428), **usage insights + 👍/👎 feedback** → admin page `/settings/assistant` (#429, KV-backed, the "see what people ask" loop), eval harness `npm run assistant:eval` (#430, 6/6 live), typing indicator (#431). **Streaming intentionally skipped** (low value on short answers + flickers partial links). **Owed:** browser eyeball on prod. **Phase 2 (grounded live-data Q&A) = the remaining big item — needs a design pass first.**

- **Contact feeder series for data (operator 2026-07-06, /feedback):** reach out to feeder championships (karting, junior open-wheel, and more) to source their schedules/results and add them as series on the site (operator outreach action, then an ingestion/curation task per series). **Refinement (operator 2026-07-06):** send each series a self-serve upload page/link that accepts their data in ANY format (CSV/Excel/PDF/JSON/etc.) — intake stays frictionless for them, landing in a staging area (Supabase Storage + a submissions row) with a normalize-then-approve step before it goes live (mirrors the threads/blog draft→approve + Clerk-role UGC pattern); the "any format" flexibility shifts the normalization/review burden onto us, so a per-series mapping/curation stage is the real work. MVP could be as light as the contact/feedback form + a file field + operator notification. **Design doc: `docs/research/2026-07-06-feeder-series-intake.md`** (intake→normalize→approve pipeline, MVP slice, 5 open decisions).

- _(also raised on /feedback 2026-07-06: per-weekend team upgrades — already tracked below, see "Surface per-weekend car upgrades on the F1 weekend page".)_

- **2026-07-06 operator follow-ups — SHIPPED (0.167.0→0.168.1, #404–#406):** completed the F1 portrait set to **22/22** (Russell/Sainz/Hamilton added; Hamilton OGL-3.0 per operator) #404 · **fixed cross-series driver slug collisions** — F1 Verstappen now owns `/drivers/max-verstappen` (portrait surfaces), ADAC 24h → `/drivers/max-verstappen-24h` (`disambiguateDriverSlugs` + 3 tests) #404 · **team page chart now plots ALL constructors** with the current team emphasized (a one-team line was pointless — operator), new optional `emphasize` prop on the shared chart #405 · **seeded official F1 highlights** for Austria R8 + Britain R9 (oEmbed-verified official channel) #406. All prod-verified. Operator decisions used: team logos = keep colour-bar (no free source); features-import = OK'd; slug scheme = F1 keeps bare slug. **STILL OPEN:** import the operator's feature-request ideas from the `/feedback` board into this Inbox — BLOCKED: reading the prod feedback DB was denied by the safety layer (I only inferred "features" = the prod board); needs the operator to allow the read or paste the items. **DEFERRED (proven pattern, worth a pass):** media.json seeding for the other ~12 series (WebSearch official channel → oEmbed-verify → add).

- **2026-07-04→06 overnight autonomous run — SHIPPED + AUDIT (0.165.0→0.166.0, #401/#402):** W4 team **points-trajectory chart** on `/teams/[slug]` (reuses `aggregateTeamsTrend`; prod-verified — McLaren line 159 == its Constructors' points) #401 · W4 **F1 driver portraits** on `/drivers/[slug]` from Wikimedia Commons, free-licences-only + per-image attribution, 19/22 drivers, prod-verified #402. **AUDIT (master pass vs current code) — these "next work" ledger items were ALREADY SHIPPED or OBSOLETE, stop resurfacing:** collapsible Champions Drivers'/Constructors' sections (DONE — `ChampionsTab` `CollapsibleSection`) · collapsible F1 race-page sections (DONE — session page wraps all 7) · historic team colours ×8 (DONE — `f1/historic-team-colors.json` already has **12** constructors) · AppShell `--tint` (OBSOLETE — the sidebar drawer was removed in 0.17.0; nothing to tint, per-page `--tint` already works). **DEFERRED with findings:** team logos = no free Commons source (only irrelevant building photos) → needs a licence decision · media.json seeding for 12 more series (existing f1/f3/wec clips AUDITED CLEAN — all official channels, live, round-matched) · portraits for George Russell + Carlos Sainz (no Wikipedia lead image via API) + Lewis Hamilton (OGL-3.0 licence, held for review). **BUG found (pre-existing, needs a decision):** `/drivers/max-verstappen` resolves to his ADAC-24h entry — `findDriverBySlug` returns the first slug match across series; needs a primary-series tiebreak (sitemap/test blast radius, so held).

- **2026-07-03 build-day SHIPPED (0.155.0→0.163.2, #373–#394) — stop resurfacing:** ranked standings rail + F1 session intervals/leader-gap + champions sparklines (#378) · per-series ICS feeds + F1 Tracks tab + route skeletons (#380) · durable last-good on 4 more standings + warm-sessions/recheck-results crons (#381) · per-session-type notif toggles + original CC0 chime + offline page (#382) · **F2 prediction markets** wired + reachable (#385/#392) · in-page **blog draft editor** (#386) + hard-404 on hidden/unknown blog URLs (#393) · driver-page Wikipedia bios + news mentions + trend chart (#390) · **social hub rebuilt in the app's flat language** (#394) · Z lint→0 + husky (#391) · **batch J home** = This-week/News empty-state consistency 0.163.3 (#90 "remaster" resolved — populated blocks were already at the polish bar) + **standings-movers widget** 0.164.0 (#94, #399: opt-in ▲/▼ since last race, F1/F3/MotoGP). **Still DEFERRED (real next work):** F2 market go-live (cron trigger) · F3 betting (needs rounds.json renumber) · grid market (dormant) · AppShell `--tint` · notif devices-list + DRY push hook + sound variants · author-role blogs + thread replies/markdown/rate-limit · team head-to-head compare page wiring · historic team colours ×8 + media.json seeds + geo-clip audit.

- **2026-07-03 full-ledger triage (109 items vs code, evidence-required):** 20 verified SHIPPED + 4 moot — full strike list with evidence in `docs/research/2026-07-03-backlog-triage-109.md`; wave-1 build batches running same day. Verified-shipped highlights to stop resurfacing: path-based tabs (0.103.0) · chequered badge (0.139.1) · push deep-links + hero images · Clerk/PWA-modal token retheme · overview.md folded into About · blog editorial plan (2026-06-27 spec) · "Past Winners" label · /just-missed warm cron (0.108.0) · home collapse (0.80.0) · champ-leader empty-set fix (0.139.0) · collapsible race sections (0.120.0) · GDPR consent banner (0.12.6).

- Bet-display refinement (operator 2026-07-03): improve how placed bets are shown — surface the multiplier, the credits-to-earn (potential return = stake × multiplier), and likely the pick/market + lock/settle state, wherever a user's bets render (weekend Bets tab bet cards, `/play` "your bets" list, `PlayMarkets`). Make a placed bet legible at a glance (staked → could win, at what odds). Scope which surfaces + what data is already on the bet/market objects vs needs adding. **→ SCOPED 2026-07-06 (attempted, reverted — needs a DECISION before building): the potential-return data is NOT on a pending bet.** `bet.multiplier` is **settle-only** (schema `20260622090000` line 107: "set at settle"; `settle_market` writes it only on `outcome='won'`), so every PENDING bet reads `multiplier=null` regardless of solo-vs-pool — verified in-browser (a fresh solo bet rendered with no odds). `UserBet` also omits `league_id`, so pending solo can't even be distinguished from pool. There is no reusable selection→multiplier resolver (the odds-key mapping is inlined write-side in `markets.ts` `selectionForMarket`; forecast is a clamped product `least(∏,500)`). **Two build paths, operator's call:** (A) **persist the fixed multiplier at placement** — 1-line add to the `place_bet` RPC (store `odds_json[selectionKey]`; null for league pools) → a new migration, **prod-gated + betting-critical**; also lets LOST bets show "would've paid"; makes null genuinely mean "pool". (B) **read-side only** — join `odds_json` + `league_id` into `getUserBets` and compute the effective multiplier per market type (no migration; must replicate the winner/podium/top10/exact/grid keys + the forecast clamp). Recommend **A** (cleanest data model; the "set at settle" note was a Phase-1 shortcut) if you're OK with a betting migration, else B. Settled-won bets can already show the real credits won with zero changes — a trivial partial if wanted.

- F1 classification speed (operator 2026-07-03): results come from OpenF1 (assembled server-side on first visit, KV-cached 7d; the 0.157.0 warm-sessions cron now captures within ~30min of session end) — audit exactly when OpenF1 publishes per session type and make results appear QUICKER: event-driven warming timed off sessions.json (poll starting minutes after each session ends instead of a flat 30-min tick), evaluate Jolpica as a faster race-classification source, and shrink the cold-path fan-out.

- Dev/staging environment (operator 2026-07-03): a `dev.paddock-tracker.com` subdomain (separate Vercel env / non-prod DB) where the operator can review + track things as an admin or moderator — draft blog posts, the thread-moderation queue, admin data — without touching prod. Operator's Clerk `publicMetadata.role: "admin"` already gates thread accept/deny + blog approval; the gap is a non-prod place to exercise it and to preview drafts before go-live. Surfaced when a queued blog draft landed on the local Supabase (127.0.0.1) instead of prod, so there was nowhere shared to review it.

- ✅ DONE 2026-07-02 (#365 interim gate → #366 fix, chart un-gated). **Corrected root cause** (the earlier theory was wrong): NOT per-round value gaps, NOT a name split, NOT a finisher-floor. The 2026 **Catalonia GP (R6) was red-flagged and restarted** — Pulselive exposes the annulled first race (`RAC`, every row 0 pts) alongside the scored restart (`RAC2`, the real 140 pts, Di Giannantonio's win). `fetchMotoGPSeasonResults` summed the first `RAC` → every scorer short by exactly their Catalonia finish (the clean GP-scale deltas). Fixed via `pickScoringRace` (use the points-bearing RAC-family session); **all 27 riders now reconcile to the standings**. The "28 vs 27 series" was Lorenzo Savadori (0-pt wildcard). NB the planned `results-overrides.json` curation would have mis-fired (keys by round → can't target the GP without corrupting the Sprint).

**▶ 2026-07-02 — ALL MERGED (Wave B #356–#362 → 0.151.0; then #364–#368 → 0.154.0).** Shipped + CLOSED (stop resurfacing): Decoder→Analysis rename (#356) · global ⌘K search + type-filter chips (#357, #364) · F1 head-to-head `/f1/compare` (#358) · owed fastest-lap + Champions a11y (#359) · NLS Results prod fix (#360) · **account-gating** = F1 analysis walled (#361) + following/home-customize walled + sign-in CTAs (#367) + `/social` + `/settings/customize` teaser landings (#368, #367) · **IA restructure** = increment 1 (Home spine pinned + "Jump to" launcher + weekend/session quick-links, #367) + increment 2 partial (top-level clickable **News**, #368) · **MotoGP chart** fixed + un-gated (#365/#366 — see the ✅ line above). **STILL OPEN:** (a) **anon visual passes** owed — home walling, `/social` teaser, weekend/session quick-links, News hover panel, launcher at 390/1024/1440 + focus order (every smoke ran signed-in); (b) **deferred IA taste calls** — Social→"Play" label, F1-Analysis top-level nav slot, Drivers/Teams nav home, per-page density pass, full jobs-to-be-done regroup (`docs/research/2026-07-02-ia-restructure.md`); (c) signed-in F1-analysis-gate pass (#361); (d) a flaky full-suite test (one transient failure, passed on re-run). See the HANDOFF top block.

- IA / discoverability rethink (operator 2026-07-01): the site now holds a LOT of info — either restructure the site's information architecture, or make the important things reachable in ~1 click from the main pages (especially the home page). Umbrella driver for the existing "IA/navigation restructure", "information-density pass", and "Home v3" items — treat those as sub-tasks of this.
- Feature gating — account-creation wall / paywall (operator 2026-07-01): put some features behind sign-up (or a paid tier) — e.g. the F1 qualifying analysis/replays (today's "Decoder"). Decide the free vs account-only vs paid split; leans on the account model the betting/social area already uses. Product/monetization decision — scope before building.
- Rename "Decoder"/"decoding" (operator 2026-07-01): bad word marketing-wise — use "Analysis" + "Replays" instead across the F1 analysis surfaces (e.g. "Qualifying Decoder" → "Qualifying Analysis"; the ghost-lap view → "Replay"; the `/f1/analysis` hub already reads "analysis"). Low-effort copy change but touches many user-facing strings — headings, nav labels, buttons, OG/SEO titles, notification copy; sweep them all.
- Collapsible Drivers'/Constructors' championship sections on the Champions tab (operator 2026-07-01): let the whole Drivers' Championship and Constructors' (Teams') Championship blocks collapse/expand — not just the decade groups within them — reusing the native `<details>`/`CollapsibleSectionHead` pattern already used for the decade groups + the changelog months.
- Global search bar (operator 2026-07-01): site-wide search for a driver, team, a specific GP/weekend, series + tabs, blog posts — "everything that is a page on our site". NEEDS CAREFUL PLANNING for fast search AND fast pages: candidate = a prebuilt static search index (drivers/teams/rounds/series/tabs/posts) shipped for instant client-side fuzzy match, or a lightweight edge search route; must not bloat the JS bundle or slow page loads. Design doc before building.
- **2026-07-01 backlog triage** (verified against current code): CONFIRMED DONE — stop resurfacing — MotoGP/WEC/GTWC live results+standings, onboarding tour (`components/Tour.tsx`), contact-form categories, custom error pages, `/api/cron/health`, decoder turns-axis, `/changelog` month-group + collapsible. Onboard any-two picker mostly exists (decoder already picks any two — only an all-lines overlay remains). STILL OPEN (real next wave) — NLS results (VLN PDF, datacenter-verify), standings last-good resilience (extend `withSourceSnapshot` to F1 standings/results + motorsport.com + warm cron), head-to-head driver comparison page, `/news` aggregated page, Sentry (needs a DSN). VERIFY-FIRST — MotoGP standings-chart under-count (likely moot, no MotoGP trend chart), `/drivers/[slug]` enrichment depth.
- Add the verified **FIA regulations link + "common topics" quick-reference to the F1 About tab** (salvaged from the closed #340 Rules-tab PR — Rules stays retired/folded into About; do NOT re-add the tab).
- Friends as its own card/page (operator 2026-06-25): on `/social`, make the add-friends + friends content a CARD like Play-with-friends / Play-solo / Read-&-discuss, labelled "Friends", linking to a dedicated page to manage friends + send/accept requests + copy the invite link; stretch — the invite "copy link" opens a native share sheet (`navigator.share`) to WhatsApp/IG/Messenger/Discord etc. (the friends graph + `/social/friends` already exist; this is an IA/card surface + a share-sheet polish). **→ SHIPPED 0.98.0 (#253):** `/social/friends` promoted from a redirect to a real page (mirrors `/social/leagues`); `/social` gains a Friends launcher card; `FriendsPanel` invite uses `navigator.share` (canShare-gated) → clipboard fallback. Browser-verified signed-in.
- Desktop nav redesign — B1/B2/B4 (operator 2026-06-24, deferred for a focused pass): hover mega-menus in AppShell (lg+) — Series→categories (reuse `lib/categories` `groupSeriesByCategory`)+clickable series; Community→Blog/Threads/News; Social→Leagues/Friends/Play; Calendar→month hover-jump (reuse CalendarToolbar's month select + CalendarView `anchorMs`). Primary nav = high blast radius; verify hover/keyboard/responsive, mobile byte-identical. **→ SHIPPED 0.97.0 (#252):** `HeaderNavMenu` disclosure primitive (hover+focus, Escape/outside/route-close); Series/Community(=Blog+Threads)/Social/Calendar menus inside `hidden lg:flex` (mobile byte-identical, verified 1440/1024/390); Calendar→`/calendar?m=YYYY-MM` (window-seeded, /calendar stays static). **News dropped** — no `/news` route exists (see below).
- Build a `/news` page (surfaced 2026-06-25 by the nav redesign): the Community mega-menu wanted Blog/Threads/**News** but there is no `/news` route — news lives only as a home block + `/api/weekend/news` + `/api/cron/news`. A dedicated aggregated-news page (cross-series, filterable) would give the menu its third destination and a real home for the news feed. Until then Community shows Blog + Threads only.

- Onboarding tour overlay (operator 2026-06-11): spotlight tutorial, ≤5 stops, auto-show once, "don't show again" in localStorage (no account needed), replay from Account. RESEARCHED — rules/verdict/build plan in docs/research/onboarding-tour-2026-06.md (verdict: hand-roll ~200 lines; Shepherd/Intro.js are AGPL-blocked). One-PR build, ready to schedule.
- Driver headshots (operator 2026-06-11): driver pages have no images. Licensing-led problem, not code: Wikimedia-sourced portraits with per-image attribution (the landing circuit-photos pattern — license+artist per file, credits rendered) — F1's 22 first, then marquee series; 600-driver full coverage is a long-tail curation program. Pairs with the deferred 0.15.0 enrichment ambition.

- Security audit (operator 2026-06-11) → PROMOTED to Now slot 2.
- Series tab switch keeps scroll depth (operator 2026-06-11) → SHIPPED PR #120 (0.24.2); Next 16's default Link scroll couldn't fix it, SeriesTabs owns the scroll.
- Drivers tab spacing (operator 2026-06-11) → SHIPPED PR #120 (0.24.2).
- Results layout v2 + weekend integration (operator 2026-06-11) → rows + clickable races SHIPPED PR #122 (0.25.0); per-session weekend results = W1c in Now slot 1.
- Results must show ALL drivers' positions (operator 2026-06-11) → SHIPPED PR #120 (0.24.2); 2-col field layout followed in PR #123 (0.26.0).
- UI/CSS inspiration pass (operator 2026-06-11, "for a little later"): review 5 reference component libraries as design-direction input — styleui.dev, cult-ui.com/docs, skiper-ui.com/components, aliimam.in/docs/components, ui.watermelon.sh. Approach: mine each for type/motion/density patterns that transfer onto the Paddock 2.0 language (not a reskin); shortlist concrete component upgrades (tables, tabs, countdown, chips) with the anti-AI principles as the filter.
- W6 — Android app via TWA wrapper (operator 2026-06-11): PWABuilder/Bubblewrap → Play Store ($25 one-time), Digital Asset Links + store assets; NOT a native rebuild. TIMING DECIDED 2026-06-11: after v1.0. iOS/App Store = separate, harder, parked.
- W7 — blog threads + UGC entries with admin approval (operator 2026-06-11): Clerk roles via publicMetadata (admin check in API routes — no Organizations product needed); submissions/drafts/approval queue/threads = relational user-writes → **the Supabase trigger (S9 fires)**. Design doc before code; absorbs the old "Fotis sit-down + provisioning + migrations" entries; don't block v1.0 launch on it. **→ SHIPPED 0.83.0 (#227)** — `/threads`: signed-in submit → admin-approve-before-public (Clerk `publicMetadata.role`); design note `docs/research/threads-design.md`. Deferred: replies, markdown, submit rate-limit.
- Historic-constructor color map (follow-up to 0.26.0 champions colors): curated per-series color file for pre-current-grid champion teams (Brabham, Lotus, Tyrrell…) so historic rows color too; 5-source rule; agent curation task.
- GTWC results round numbers: parser emits raceId/eventName but no canonical round — blocks weekend links on GT World results rows (noted in 0.25.0). Align with rounds.json.
- FE doubleheader second races have no weekend URLs (R8/R10-style rounds share the weekend page of the first race) — modeling gap surfaced by 0.25.0 link gating; 8/10 FE races link today.
- Endurance-series weekend grouping audit (demoted from Next by the wave triage): WEC / IMSA / NLS / ADAC 24h races + multi-day tests vs `groupByWeekend`'s 4-day gap heuristic.
- Curation patches when timetables drop (demoted from Next): IMSA Practice 1 on R6–R11, FE Sanya R11 session times.
- Track B leftovers (demoted from Next): B11 path-based tab routes `/series/[slug]/[tab]` (1–2 days), B12 Greek `/el/` route tree via next-intl (3–5 days).
- Research live in-race data feeds (sector times, leaderboard, gaps, tyre choices) for F1, MotoGP, WEC, FE, IndyCar.
- Reverse-engineer fiaformulae.com / motogp.com / nascar.com XHR endpoints to see if unsigned JSON can replace Playwright.
- Decide Sandbox/Playwright vs third-party aggregator vs curation-first for JS-rendered official sites.
- Enrich /drivers/[slug] with Wikipedia bio, current standings position, last 5 results, news mentions.
- Enrich /teams/[slug] with the same shape.
- Redesign F1 History tab or replace with curated content/series/f1/history.md.
- Improve Rules tab with FIA PDF link and a "common topics" surface.
- Implement lib/results/<slug>.ts and lib/standings/<slug>.ts for MotoGP, WEC, IndyCar, NASCAR.
- NASCAR trend chart polish — 47-driver legend cluttering, leader-vs-tail spread crushes the bottom cluster, Y-axis only labels 150 and 600. Fix candidates: cap legend to top-N drivers, drop "(i)" / "(R)" suffix from labels, more Y-axis ticks, optional log scale or zoom-to-leaders default. Operator-flagged on /series/nascar-cup?tab=results 2026-05-22.
- TheSportsDB free tier evaluation done 2026-05-22 — skip, doesn't cover NASCAR/WRC/DTM/F1/MotoGP/IMSA/WEC/IndyCar/FE on free key, only V8/BTCC/British GT/WorldRX/WorldSSP. Paid Patreon (~$10/mo) might unlock more but data is schedule-only on free tier. Do not re-evaluate.
- Sportmonks F1 API — only commercial source with lap/pit/stint/livescore data for F1. F1-only and ~€19+/mo. Park until Paddock builds a live-timing roadmap; revisit then.
- API-Sports F1 v1 — alternative to Jolpica if Jolpica becomes unreliable. F1-only, 100 req/day free tier. **Docs page 403s datacenter IPs** (same CF failure mode as racing-reference); MUST be tested from a Vercel preview before any adoption attempt.
- Add a custom app/error.tsx page.
- Integrate Sentry for error monitoring.
- Add /api/cron/health summarising last-run timestamps for every cron job.
- Run a Lighthouse and Speed Insights perf audit and act on findings.
- Make it faster — explicit performance pass beyond the audit (image optimisation, bundle size, hydration cost, route-segment caching, Suspense boundaries).
- App best-practices pass — error boundaries on every route segment, loading states, Next.js 16 idiomatic patterns (cache directives, segment configs), proper Suspense boundaries with sensible fallbacks.
- Fix the nine legacy ESLint errors and add a husky pre-commit hook.
- Add component tests with vitest + Testing Library.
- Add Playwright E2E tests that run on Vercel preview deploys.
- Build a comments thread (Clerk-gated) on race-weekend pages.
- Build predictions with an open → locked-at-session-start → resolved-after-race state machine.
- Persist active news-filter chip across page reloads.
- Run a mobile-first UI/UX audit using the tailwindcss-mobile-first patterns.
- Run a WCAG 2.2 AA accessibility audit and fix gaps.
- Polish motion, focus states, and dark-mode contrast across the site.
- Run user research via a site survey, conversations with fans, and subreddit pain-point mining.
- Research about consumer mindset and psychology to inform what we're building and how it's framed.
- Add per-event-type push notifications (qualifying topper via RSS filter, race winner, championship-deciding event).
- Custom notifications — per-user rules (e.g. only notify for F1 + MotoGP race day, skip practice).
- Notification sound refinement — per-series default sound, per-type variants, optional pre-race chime vs news ping.
- Notification badge icon refresh — chequered-flag motif on `public/icons/badge-96.png`. Current monochrome is functional but generic.
- Make the push click handler deep-link to a specific session or article instead of always opening /.
- Build a Settings "Your devices" list with per-device test and remove buttons.
- Send hero images in push payload.image, sourced from curated circuit JPEGs or motorsport.com thumbnails.
- Investigate per-series Champions JSON to fix fragile parser (F1 wrong points column, F3 all zero, MotoGP brittle redirects).
- Delete unused lib/onboarding.ts (only wizard-reopen consumer).
- DRY the duplicated logic between EnableNotifications and OnboardingWizard.
- Retheme the Clerk sign-in/sign-up pages + UserButton to Paddock 1.0 light + dark.
- Migrate the remaining PWA-only modals (OnboardingWizard, EnableNotifications, PWAInstallPrompt, NotifPrefsSection, SettingsClient) to Paddock 1.0 tokens.
- Migrate `components/mdx/mdx-components.tsx` to Paddock 1.0 tokens.
- Add a WeekendMedia section to /series/<slug>/weekend/<round> fed by content/series/<slug>/media.json (YouTube highlight reels, blog cross-links, onboard clips).
- Choose an embedded-video provider (YouTube iframe vs Mux vs Cloudflare Stream).
- Embed YouTube highlights + extended-highlight videos on past weekend pages, and add dedicated season / month recap pages with embedded season-highlight videos + written blog text + standings snapshots at that point.
- Add a Tracks/Circuits view per series with a map.
- Make the home hero show the next 2–3 sessions when all are imminent.
- Make session cards tap-to-expand to broadcast info, streaming, and track details.
- Add per-driver season-trend chart on /drivers/[slug].
- Add era markers and sparklines to the Champions tab.
- Fold overview.md content fully into the F1 About tab.
- Surface "common topics" on the Rules tab.
- Rotate sk_live_* Clerk keys.
- Surface per-weekend car upgrades on the F1 weekend page — what each team brought to this round. **→ DATA SOURCE FOUND + VERIFIED 2026-07-06:** the FIA **"Car Presentation Submissions"** PDF (Document 13, one per GP) is the official structured source — downloaded 200/3.4 MB + `pdftotext -layout` parses clean per-team tables (component / reason [Performance·Geometric·Circuit-specific] / description). URL: `fia.com/system/files/decision-document/2026_<gp>_-_car_presentation_submissions.pdf` (or scrape the season portal listing). **RapidAPI has NO upgrades endpoint — that candidate is dead (corrects the prior note).** F1.com per-race articles = robots-allowed but prose (cross-ref only); PaddockIntel = proprietary JS SPA (skip). Recommend curation-first → `content/series/f1/upgrades.json` → weekend section; automate later, gated on a Vercel datacenter-IP probe of fia.com. Full writeup: `docs/research/2026-07-06-f1-upgrades-data-source.md`.
- Live in-race data feed (lap-by-lap, telemetry, sector splits) for the live-now view on race day. Implementation hint: RapidAPI's "F1 Live Timing - Telemetry and GPS" is one candidate; canonical long-term path is Pulselive backend for MotoGP/WSBK + Jolpica live extensions for F1.
- Per-session results-fetch lifecycle — when a session ends, fire Phase 1 (positions, times, basic video/news → `result` table); fire Phase 2 hours-to-days later (interviews, race reports, highlight videos appended to weekend news + media). Uses `session.end_instant_utc` as the trigger boundary. Implementation: Vercel cron sweep + `results_fetched_at` dedupe; not literal per-session timers. Formula E is the first concrete target — no results coverage today.
- Head-to-head comparison for drivers — pick two, see season stats, qualifying / race deltas, fastest lap, points trajectory. Possibly delivered as a magazine-style page or article.
- Head-to-head comparison for constructors — same shape but team-vs-team.
- Calendar UI — month-tabbed / swipeable view instead of one massive scroll per series. Pairs with mobile-first audit. **→ SHIPPED 0.65.0 — interactive Month / Week / Day views on /calendar.**
- Contact form categories — dropdown for "Bug report" / "Feature request" / "Suggested change" / "General" so submissions self-triage. Today the form is plain email + body.
- Admin UI for adding pages + content — lightweight authoring interface beyond the conversational "Claude edits files + commits" flow. Useful when Claude isn't available or for Fotis-driven content updates.
- IA / navigation restructure pass — easier mental model. May follow user research. Suspect candidates: nest some tabs, surface "current weekend" at top of home, regroup series by category (formula / motorcycle / endurance / oval / rally).
- "Live Now" section improvements — currently a thin pinned strip. Expand to show current session / lap / leader / gaps when live, not just "Live now: F1 Practice 2".
- Champions tab visual redesign — better card layout, era groupings, image avatars. Pairs with the Champions JSON cleanup.
- News articles content pipeline — beyond MDX blog scaffolding (shipped 0.8.0), set editorial direction + draft first posts. Tied to Parked item "Write first 2–3 MDX blog posts".
- Offline mode — service worker caching of the next 7 days of weekend data so the PWA still renders on the underground or at a circuit with no signal. Pairs with the existing serwist setup.
- Split Web app from a real Play Store / App Store wrapper — defer until there's profit motive. Currently the PWA installs as a homescreen app on both platforms which is good enough for v1.
- Lift `--tint` to `<html>` so the sidebar drawer's active series link picks up the series accent on /series/<slug> routes — currently it stays global signal-amber because the sidebar is outside the page-wrapper scope.
- Sidebar `--tint` follow-up — requires a server-side route lookup per request to determine the active series.
- Champions tab label for singleEvent series — rename "Champions" → "Past Winners" since it's functionally a list of 24h winners rather than season champions.
- WeekendMedia content seed — `content/series/<slug>/media.json` with YouTube highlight IDs + circuit imagery for the next 4 race weekends.
- User-authored blogs + threads with review-then-publish moderation flow; likely needs Supabase auth + role-based privileges; research existing UGC blog platforms (Substack, Ghost, Discourse threads) for prior art before designing.
- Per-series calendar feeds — webcal/ICS endpoint (e.g. /api/calendar/<slug>.ics) proxying curated session data so fans subscribe in Apple/Google Calendar; unlocks the mockup's "Sync your calendar" perk card on the landing (cut from 0.13.1 for honesty).
- Results re-check lifecycle (operator 2026-06-12): re-verify session results at +1w / +1m / season-end because penalties get overturned late — Gasly's Monaco podium reinstated ~a week after the race and Jolpica was still pre-correction on Jun 12; shape = KV results snapshot + scheduled diff cron + curation alert, pairs with results-overrides.json.
- OpenF1 anonymous lockout during live F1 sessions (found 2026-06-12): API 401s ALL endpoints incl. historical whenever a session is live, so F1 per-session classifications break exactly on race weekends; fix = persist-once to KV (+ optional €9.90/mo sponsor key as belt-and-suspenders). → PARTIALLY ADDRESSED 0.39.1 (PR #159): captured session classifications now KV-persist (7-day TTL) and render through the lockout; residual = a cold/expired entry first opened *during* a lockout still can't fetch.
- Pre-warm cron for weekend session pages (follow-up to 0.39.1): ping past-session classification pages (or write directly to the session-class KV) so cold/expired entries are already cached before any live OpenF1 lockout — closes the residual gap above; pairs with the /api/just-missed cache-warm cron candidate.
- Information-density pass (operator 2026-06-12, "serious discussion"): multiple users report pages show too much — needs a per-page information-budget review (what question does each page answer in 5 seconds; rest behind disclosure); pairs with W5 per-page layout spec.
- Home v3 (operator 2026-06-12, direction agreed): two-block home — "Just missed" (results + article link + later media) and "Up next" (countdown + official stream link) with ranking/caps (followed-first, hard cap ~3, hero + quiet rows); news demoted to secondary strip; spec to be written as the W5 design session AFTER audit highs land (MotoGP/FE weekend-URL fixes + device-local times gate it).
- Landing scroll-driven animation set-piece (operator 2026-06-12): "as you scroll an F1 car comes near the screen" — draft-form prototype first; candidate tech: CSS scroll-driven animations (animation-timeline: scroll) or canvas image-sequence scrub; landing-only, prefers-reduced-motion static fallback, perf budget (no jank on mid phones); sequence after home v3.
- Android app dedicated discussion (operator 2026-06-21): revisit TWA/Play Store status — closed test, Play App Signing fingerprint to append after the .aab upload, branded offline route + serwist fallbacks, manifest id/scope/shortcuts.
- Minigames (operator 2026-06-21): "guess the driver" and "guess the track" from photos of top drivers per category, "guess next turn" (show one corner of a circuit, user guesses if the next corner is left or right), and more — engagement/retention feature.
- Re-look into the curated PRs for post-session blog posts (operator 2026-06-21).
- Copyright-free photos of ALL drivers + ALL team logos (operator 2026-06-21) — licensing-led curation program (Wikimedia + per-image attribution, the landing circuit-photo pattern); feeds driver/team pages, the JUST MISSED block, and the minigames.
- Race-weekend track-map photo showing sectors/corners (operator 2026-06-21) — per-circuit sector diagram on the weekend page.
- /api/just-missed cold-on-cold TTFB ~13.8s (audit 2026-06-21): warm HIT 0.44s but full season-results fan-out (WEC live-component + MotoGP no parser cache) runs when edge + KV podium caches both cold; fix candidates = cache-warm cron pinging the route, or add parser-level KV cache to lib/results/motogp.ts.
- Geo-restricted highlight clips (operator 2026-06-21): some curated WeekendMedia highlights in content/series/<slug>/media.json point to unofficial/random YouTube channels whose uploads are region-locked (won't play outside their country); audit every curated clip's global availability and replace with official-channel uploads (FIA WEC / F1 / etc.) per the search-official-source rule.
- Paddock Betting game (S9 trigger, operator 2026-06-22): virtual-credit betting (NO cashout — the anchor), free + optional paid IAP, win-rate leaderboard, persistent deliberately-lean bankroll, pari-mutuel + model + odds-API hybrid pricing; full spec + locked decisions in `docs/research/predictions-design.md`; gated on Supabase provisioning + legal review (paid path = simulated-casino category, geo/age gated). **→ SHIPPED — betting LIVE end-to-end since 0.46.0; ongoing refinement tracked in Now §1.**
- Native Android rebuild spike (operator 2026-06-22): built + flashed to Pixel 9 (`C:\Dev\Personal\paddock-android`, Compose + /api/just-missed); Android toolchain installed cold; polish parked (chequered-flag icon + Paddock theme); proves on-device feasibility, NOT that the full 15-series/auth/push/content rewrite is cheap — detail still punts to web.
- MotoGP standings chart under-count (0.41.0 follow-up): chart sums 132 vs standings 157 (Di Giannantonio) — a round/session dropped under the finisher floor in `fetchMotoGPSeasonResults`; fix → MotoGP joins the F2/F3/WSBK chart set.
- Standings last-good resilience (operator's "both #2", owed): KV fallback so a transient motorsport.com/datacenter failure can't blank standings; also softens cold-load delays across motorsport.com-sourced series.
- NLS Nürburgring results (operator 2026-06-21): no results today; Phase-1 source = teilnehmer.vln.de PDF; new scraper, DTM-shaped, datacenter-verify required.
- Session-page → series-tab navigation (operator 2026-06-22): from a weekend/session page, reaching a series' Standings is "too far back"; breadcrumb isn't an obvious back-path — IA polish, pairs with path-based tabs (B11).
- Remaining standings charts FE/IndyCar/GT-World/IMSA/WEC (after MotoGP): data-gated — winners-only / no per-position points; GT-World/IMSA/WEC need a per-series points-scale module before a chart can reconcile.
- Standings charts = ranked name + points list (operator 2026-06-22): each chart should show driver name + points sorted descending (most points at top → least at bottom), not only a points-over-time trend; refines the MotoGP + remaining-series standings-chart items above.
- Betting — open more markets across more SERIES (operator 2026-06-22): lead-time shipped 0.48.0 (next 3 F1 weekends open at once); remaining = non-F1 series, gated on per-series winner-race disambiguation (F2/F3/MotoGP/WSBK have sprint+feature) + standings↔results driver-name verification + datacenter check (blockers in HANDOFF).
- Betting — new market types (operator 2026-06-22): PODIUM + TOP-10 + EXACT-POSITION engines shipped dormant (0.49.0–0.51.0) + multi-market UI (0.50.0) — pricing + settle migrations + verify scripts done, not auto-opened (go-live steps in HANDOFF); exact-position is engine-done but UI-blocked (2-field driver+position pick needs a bespoke selector); remaining = grid/qualifying-position (quali-pace model + a `market_type` enum addition). **→ podium + top-10 LIVE 0.54.0; the exact_position picker UI was subsequently built (0.53.0 — corrects the "UI-blocked" note), so go-live is now Now §1(d) (interaction-verify signed-in + add to `MARKET_BUILDERS`); grid/quali stays here, gated.**

- Betting — odds from real betting apps, not the standings model (operator 2026-06-22): bet multipliers should come from a real bookmaker odds API, not the championship-points model. Hybrid — books price F1 race winner (+ maybe podium/points-finish) but NOT exact-position/grid, so the model stays the fallback for those; still clamp API odds through the house band (MIN 1.3 / MAX 30 + margin) so the no-900× economy holds. Needs a provider + paid-key decision + provider↔standings driver-name matching. This is the long-planned "odds-API adapter" (predictions-design.md); pricing.ts already has a clean seam (winMultipliers etc. feed createMarket). **→ PROMOTED to Now §1(c).**

- Leagues overhaul (operator 2026-06-22, APPROVED + building): dedicated league page, per-league settings, per-member nicknames + colours (anyone can set for anyone), invite LINKS (not just code) that prompt account-create + join + a friend request to the inviter (prompted accept, not auto), month/season prizes = titles/badges for top 3. Needs a global friends graph (in-league you can friend non-friends). Phased: **P1 friends DONE 0.55.0** (`friendship` table + `/api/friends` + `/play` Friends section + verify), **P2 invite links + join-&-befriend DONE 0.56.0**, **P3 league page + nicknames/colours/settings + owner disband/kick DONE 0.57.0**, P4 prizes. Friends global; leagues invite-only. **P4 prizes DONE 0.58.0 → leagues overhaul COMPLETE.**
- Dedicated Social area (operator 2026-06-22, IA locked): friends + leagues are NOT children of /play; play stays /play. Group them under a new `social` area — `/social/friends` + `/social/leagues` (+ a `/social` index), relocate league detail to `/social/leagues/[id]`. Post-P4. **→ SHIPPED 0.59.0 (area + Friends|Leagues sub-nav + 308 redirects on old `/play/leagues/*`) + 0.60.0 (friend search/add/remove).**
- Landing-page marketing for the betting/leagues/social game (operator 2026-06-22): the predictions + friend-leagues feature needs a marketing section on the landing page to drive sign-ups — plan a solution (honest no-cashout social-game framing). **→ PROMOTED to Now §1(a) — lead refinement item.**
- Richer league leaderboard (operator 2026-06-23, brainstorm later): the leaderboard should surface more than name + W/L + win-rate — candidate columns to brainstorm: total staked/returned (net credits), biggest single win, current win/loss streak, last-N form, podium/title (honours) count, member colour dot, # bets this period; needs a design pass + which are cheap from existing data vs need new aggregation. **→ PROMOTED to Now §1(b) — design pass first.**
- Home widgets + layout modes (operator 2026-06-24, "we'll see"): new home blocks users can add/order beyond the current three (chyron / just-missed / schedule) — candidates: your bets + credits, a mini league leaderboard, next-race weather, a standings snapshot, championship leader, a "from the blog" card; PLUS a layout-MODE dimension ("how they sit" — stacked cards vs side-by-side boxes vs density). Extends `HomeLayoutPrefs` (order/hidden/collapsed) with a `mode`/`columns` field + a per-widget registry; the Account customise banner + live preview (0.73.0) already exist to drive it. **→ PARTIAL 0.85.0 (#230): customise moved to its own page `/settings/customize` with a widget-discovery gallery; the new widget TYPES (per-series countdowns, track layout, standings snapshot, championship leader, from-the-blog) render as "Coming soon" cards — wiring them into `HomeContent` + a `mode`/columns dimension is the remaining work.** **→ COMPLETE 0.102.0–0.107.0: every widget TYPE shipped (from-the-blog, championship-leader, standings-snapshot, per-series countdowns + results, circuit-map) PLUS deep per-widget customisation (0.105.0 — per-widget settings + density). The `mode`/columns layout dimension was NOT built — revisit only if a real need surfaces.**
- Home collapse for more blocks (follow-up to 0.73.0): collapse ships for Just-missed only; extend to Schedule (its 2-col composite needs an umbrella header) and optionally the chyron — flip `collapsible: true` per element + reuse `CollapsibleSectionHead`.
- League-member → profile links (follow-up to 0.76.0): the member rows in `LeagueDetailView` (+ honours names) should link to `/social/users/[id]`, like the friends list now does.
- Legacy lint cleanup (surfaced 2026-06-24): 5 pre-existing `react-hooks/set-state-in-effect` errors remain (OnboardingWizard ×2, FriendsPanel, CalendarView, + 1); `useHomeLayout`'s was fixed in 0.73.0. Fix where each file is next touched, or a dedicated chore PR; pairs with the parked husky/CI item.
- Account analytics / admin view (operator 2026-06-24, "we'll brainstorm"): now the operator has Clerk admin, surface admin-only data in Account — site traffic/visits (GA4 already wired — GA4 Data API vs a lightweight KV counter?), user counts + signups, betting/engagement stats. Brainstorm which metrics + how to admin-gate before building. Pairs with the Clerk-role pattern threads already uses.
- Generalised upstream-feed resilience (operator 2026-06-24, off the Jolpica outage). **→ SHIPPED the DB layer 0.91.0 (#237):** `source_snapshot` durable last-good + per-source health + `withSourceSnapshot`; **news wired through it**. (Revised the earlier "DB-as-cache rejected" call — for ISR/feed reads the eu-west-1 hop is amortised once per revalidation, and a table gives a durable last-good + health record KV can't.) **Remaining:** extend `withSourceSnapshot` to F1 standings/results (off the 0.84.0 KV last-good) + the motorsport.com scrapes, and add a warm cron so the request path never hits upstream cold. Supersedes the older "standings last-good resilience" item.
- **Forecast market went LIVE 0.88.0 (#234)** — operator decision (Now §1e); exact_position still held (Now §1d). Watch the first real forecast settlement after an F1 race. **→ multi-leg picker verified signed-in 2026-06-24 (Clerk dev keys + Playwright).**
- **Blog pipeline (operator brainstormed + LOCKED 2026-06-24 — FIRST task next session):** DB `post` table (status draft/approved/published, `publish_at`, series_slug); draft → admin-only push ("ready to review") → admin approves early → post hidden until `publish_at` → a cron publishes it + fires the all-users push. I draft (preview + recap per weekend; cited sources, no-BS) on a **scheduled trigger** (ScheduleWakeup / cron / `/loop`). = threads moderation + a scheduled-release layer + two notify audiences. Full design in the HANDOFF top block. **→ SHIPPED 0.92.0 (#240)** — infra live + verified (prod migration via the Management API; the Dublin `health` workflow green; publish cron green). Owed: a signed-in push-walkthrough + first real content + the scheduled-authoring trigger.
- Free attributed imagery program (operator 2026-06-24): Wikimedia Commons (per-file license + attribution, the landing circuit-photo pattern) for driver portraits, blog photos, AND **series logos** (find non-infringing versions for all 15). **NO Getty** (it's copyrighted/licensed — operator's "not copyrighted" was wrong). F1 first; long-tail curation.
- Drop the F1-radio notification sound (operator 2026-06-24): likely copyrighted — replace the `PushSoundPlayer` cue with an original / CC0 sound (generate or source one). Pairs with the imagery-licensing cleanup.
- Calendar day view "order by" control (operator 2026-06-26): when a day is opened (e.g. Sat 27 Jun 2026), add an order-by toggle to sort that day's sessions by series or by time. **→ SHIPPED 0.101.0 (#262).**
- Remaster the existing home widgets (operator 2026-06-26): visual/design refresh of the current home blocks (chyron / just-missed / this-week / news) — raise them to the polish bar of the new gallery widgets; pairs with the information-density pass.
- Beautify the release-notes / changelog page (operator 2026-06-26): redesign `/changelog` (rendered from RELEASES.md) — batch releases into weekly or monthly groups with a nicer visual treatment, instead of the current flat newest-first list.

- Deep per-widget customisation SHIPPED 0.105.0 (PR #266): per-widget content settings + density via a gear disclosure on /settings/customize; HomeLayoutPrefs.config generalised to a per-widget settings map (v6) with the pre-v6 snapshotSeries migration; snapshot Series picker scoped to followed. Minor follow-up: championship-leader with ALL series deselected renders an empty block (no "pick a series" message) — treat an empty seriesSet as "all", or add an empty-state line.
- Per-series home widgets SHIPPED 0.106.0 (PR #267): "Series countdowns" (one live countdown per followed series) + "Series results" (latest result per series), both opt-in with a per-widget count; also fixed chyron density being a no-op (compact now tightens its padding).
- Circuit map (track-layout) home widget SHIPPED 0.107.0 (#269): Approach A — F1-first static f1db circuit SVGs (CC BY 4.0), card reframed from "corner and DRS detail" → "the circuit map for the next round" (DRS is F1-only + in no dataset). Sources + data model in `docs/research/2026-06-26-track-layout-scope.md`. **Deferred:** Phase 1b = curated F1 corner/DRS overlay; Phase 2 = multi-series outlines via track-atlas. Nuance: a follow-all user sees whichever series has the soonest session at a mapped circuit labelled (e.g. F3 at a shared F1 track).

- Collapsible regions on the F1 race session page (operator 2026-06-29): the race page now stacks 7 sections (Classification → Race Story → Tyre strategy → Moments → Speed Trap → Pit-Stop League → Overtakes, from #288) — wrap the major sections in collapsible disclosures (reuse CollapsibleSectionHead). **→ DEFERRED 2026-06-29: justified + ready, but the collapse interaction needs a browser to verify (MCP browser was locked all session) — build on a preview pass / unlocked browser.**
- Home-widgets pack SHIPPED 0.115.0 (#289): where-to-watch + next-race weather + driver-spotlight (opt-in, multi-series, default-hidden) — the audit's "ready now" widgets. standings-movers still deferred (needs round-over-round standings history).
- F1 telemetry leaderboards SHIPPED 0.116.0 (#288): speed-trap + pit-stop league + overtakes-of-the-race on F1 session pages (free OpenF1, historical; on session pages NOT home → no F1-skew). Tyre-strategy was already in Race Story. Closes the OpenF1 widget backlog.
- Blog author bylines + full draft preview SHIPPED 0.117.0 (#290): "By {author}" under the title (from author_id, all posts); admins can read a whole draft from the /blog queue (↗ to the full rendered preview) before approving.
- F1 driver headshots SHIPPED 0.118.0 (#291) via OpenF1 on /drivers/[slug] (F1-only). ⚠️ COPYRIGHT: images are F1 official media (media.formula1.com) — OpenF1's CC data licence doesn't license them; isolated to one swappable fn (`f1HeadshotsByNumber`). FOLLOW-UPS: swap to a Wikimedia/own-licensed source; team logos + non-F1 driver photos still need the licensing-led imagery program.
- Austria GP recap QUEUED 2026-06-29 as a draft (slug `austrian-gp-2026-recap`, id `fa7cb781`) — data-driven off OpenF1, triple-audited; operator approves it in the /blog review queue (once 0.117.0 deploys) to publish + push.
- Blog byline now resolves name + avatar from CLERK (0.119.0) — fixes the byline reading the stale `app_user.display_name` + adds the author's profile photo next to "By {name}" (the #290 byline read `app_user.display_name`, which diverged from the user's Clerk account).
- Let select (non-admin) users write blogs (operator 2026-06-29): a role-gated "author" — vetted users submit drafts → admin approve → publish (reuses the existing draft→approve→cron flow + the new full-draft queue preview). Builds on W7's Clerk-role pattern (threads). Needs a role + a submission UI.
- Enrich blog posts with data visuals (operator 2026-06-29): links to / screenshots of the relevant standings, copyright-free track images (the f1db circuit SVGs in `public/circuits` are CC BY-4.0), and embedded graphs (reuse SeasonTrendChart / stint charts) — for the Austria recap + future race posts.
- /feedback board worked 2026-06-29 (operator) → full dispatch detail in the HANDOFF FINAL block. DONE: Austria recap (queued 3pm), next-race fix (#296), 3D-quali fix (#300, verified live), landing-hero mobile width (#300), account profile-pic (#300), notifications remodel (#295+#297), friends/leagues home widget (#298), practice-session telemetry (#299). OPEN (next session, PARALLEL lanes): (A) /series "too wide" — clarify which tab (no calendar-tab overflow) + /social "weird cards" — need a signed-in screenshot; (B) 3D track comparison + throttle/brake "did Russell lift" — F1 feature, now unblocked; (C) "better bet handling" (awards/leagues/screens/info/data) — scope first; (D) race-weekend highlights → official accounts only — media.json curation; (·) Loutris's PWA won't open — need device + symptom.

- Session/results table polish (operator 2026-06-29): on results, hover-highlight a row so the driver + time read clearly on big PC screens, AND add an interval column (gap to the car in front) + a leader-gap column; scope which results surface (F1 OpenF1 classification vs the per-series Results tab) + per-series interval/gap data availability first.
- Onboard/T-cam on the followed car (operator 2026-06-29) — SHIPPED 0.130.0 as a Chase⟷Cockpit toggle on the onboard replay (cockpit = camera above the driver's head, looking over the nose, wider FOV; Chase stays default for comparing). Kept TIME-sync per the operator ("where the cars were at the same elapsed time"). KNOWN LIMITATION (operator informed): under time-sync the rival is 15–40 m away = usually around the next corner / behind you, so in the narrow cockpit frustum it's a glimpse on the straights, not constant — Chase is the comparison view, Cockpit is immersion. REMAINING FOLLOW-UP: an off-screen ghost indicator (edge arrow + gap) so the rival is locatable in cockpit when out of frame; the throttle/brake strip already carries the detailed comparison.

- Onboard comparison — broadcast cameras + all-driver roster (operator 2026-06-30) [→ NEXT ONBOARD PHASE — unblocked 2026-07-01 by the shipped 0.132.0 overhaul; the reconstructed-track + real-car + procedural-env foundation is now solid, so this is mostly the picker + per-driver line caching + the director logic]: (P1) lay ALL drivers on the reconstructed track individually with their own racing line + lap time, pick any two to compare — mostly an extension of the existing per-driver decoder traces + a picker + per-driver line caching (~20 location fetches, cache-hard); optional faint overlay of all racing lines. (P2) preset broadcast-style fixed trackside cameras at advantageous spots (auto-placed at the high-curvature corners of the reconstructed centreline, elevated, aimed at the apex) as a third View alongside Chase/Cockpit. KEY DESIGN CATCH: a truly static cam shows the cars as tiny dots most of the lap (cars ~0.08u on an ~11u track) — so it needs pan (lookAt the cars) + zoom (FOV by distance) + an auto-DIRECTOR that cuts to the nearest camera; the director logic is the real work, not the placement. Framing BOTH cars when far apart (time-sync) has the same tension as the chase cam (zoom out / follow leader). Spec via brainstorming before building.

- Onboard 3D graphics overhaul SHIPPED 0.132.0 (PR #323, 2026-07-01): the qualifying onboard is now realistic on every quali — real CC-BY car sized to the true 1.9m/~12.5m ratio of the MEASURED track (+ camera rig scaled to match so the cockpit T-cam framing holds); ONE inverse-distance-weighted ground the track sits on (no grass-over-track); grounded continuous barriers (no flying/tilted panels); outfield-only trees cleared of every track section (none on the racing line); procedural pit lane + garages on the main straight; mobile 4x-speed-button wrap fix. Built + verified via operator-visible localhost screenshots (the visual gate 0.131.0 skipped) + operator live sign-off. Full detail + workflow in `docs/HANDOFF.md` (2026-07-01 block). FOLLOW-UPS (polish, low priority): pit is generic (enhance garages/lane readability); a downhill-outside barrier can read as a tall retaining wall where the ground falls away; tyres are still team-coloured (darken). Real-geometry P2 (TUMFTM/Umeyama) stays the marquee-circuit roadmap (`docs/research/onboard-3d-rebuild.md`); the broadcast-cameras + all-driver item above is the next onboard phase.

## Parked (might do, with a revisit trigger)

- **GitHub Actions CI workflow (typecheck + vitest on PRs).** Why parked: prior CI attempts on other repos always failed on the first PR. The shape is ~20 lines and unambiguous, but the user has zero tolerance for noisy red checks. Revisit when we can pair-debug a known-green workflow on a throwaway branch first, *then* merge it. Likely root causes when we get to it: action-version drift, `npm ci` vs `npm install` on lockfile mismatch, GITHUB_TOKEN scope, or branch-trigger mismatches.
- **Paddock-coins ledger + leaderboard.** Why parked: depends on Supabase + comments. Revisit after S9 (race-weekend Part 2) lands.
- **Public README with screenshots + Mermaid architecture diagram.** Why parked: post-v1.0 polish, has zero user-facing impact today. Revisit when prepping for LinkedIn showcase.
- **Write first 2–3 MDX blog posts.** Why parked: blog scaffolding is shipped (0.8.0) but no content yet. Needs an editorial direction first. Revisit when user has time for writing.
- **Another "Claude design" depth pass** for background warmth and global theming. Why parked: Paddock 1.0 restyle just shipped (0.10.0). Revisit after the next user-research pass surfaces specific complaints.
- **Era markers / sparklines on Champions tab.** Why parked: relies on having clean curated champions data per series; current scrape is fragile. Revisit after the Champions JSON cleanup.
- **GDPR / cookie-consent banner for Google Analytics 4.** Why parked: shipped 0.9.15 GA4 sets cookies without consent — fine for ~30 visitors/day, not fine at scale. Revisit at ~500 visitors/day or a real legal complaint.
- **B8b — SoftwareApplication JSON-LD on `/`.** Why parked: per `docs/seo-geo-playbook.md` Part 4, semantic fit is perfect (`WebApplication` + `applicationCategory: "SportsApplication"`) but the schema's `aggregateRating` / `review` requirement is a hard blocker. Emitting without yields invalid markup + zero rich-result eligibility. Revisit when Paddock has either real user-review infrastructure or at least one editorial review to cite. Annual re-evaluation.
- **Supabase region move (Dublin `eu-west-1` → Frankfurt `eu-central-1`)** (operator 2026-06-24). Why parked: **counterproductive while Vercel compute is pinned to `iad1`** (no Pro). Dublin is the closest EU region to iad1; Frankfurt is *further*, so per-query latency would rise, not fall. Region also can't change in place — it's a new project + data migration + env swap (PAT-gated). Revisit only if compute moves to the EU (`fra1`/`dub1`, needs Pro) — co-locating compute with the DB is the real latency lever; until then, caching (0.72.3) is it. **UPDATE 2026-06-24: premise gone — the operator moved Vercel compute to Dublin (`eu-west-1`), now co-located with the DB. The latency lever is realised; a Frankfurt move would only add distance. Effectively closed.**
- **Move betting data to Cloudflare D1** (operator 2026-06-24). Why parked: **not lighter from Vercel/iad1** — D1 is reached over HTTP like Postgres, so the transatlantic latency is unchanged, and D1's single-writer SQLite + 10 GB cap can't host the atomic credit-ledger / `place_bet` / `settle_market` RPCs + triggers the economy relies on. KV already fills the light read-cache role. Revisit only as part of a full platform move to Cloudflare Workers (edge compute + D1 + KV) — a migration, not a table move. **UPDATE 2026-06-24: the "unchanged transatlantic hop from iad1" premise is gone (compute is now Dublin); D1 still can't host the atomic ledger / RPCs, so it stays parked on that basis alone.**
- **"All page content in the DB" + idle-prefetch of hidden segments** (operator 2026-06-24; ESPA'd → parked). The **storage half is rejected**: static/ISR pages served from the CDN edge beat any DB read (even a now-local Dublin one), it's a multi-week rewrite of the data layer, and it loses "conversational authoring is the CMS" (git-versioned, PR-reviewable content). The original "DB is a transatlantic hop" objection is now moot (compute moved to Dublin) — but the other two stand. The **idle-prefetch half is the keeper**: warm the most-likely-expanded collapsed-segment endpoints on `requestIdleCallback` (connection-aware, deduped, cache the result) so an expand feels instant — Dublin makes those warmed endpoints cheap. Revisit as a **B-perf sub-task** (Now §3).

## Killed (won't do — with one-line why)

- **Paddock-coins ledger + leaderboard** (killed 2026-07-03): superseded wholesale by the betting credits economy (`credit_ledger`, league leaderboards, monthly awards).
- **Supabase region move + Cloudflare D1 move** (killed 2026-07-03): Dublin compute co-location realised the latency win; D1 still can't host the atomic ledger RPCs.
- **Reverse-engineer fiaformulae/motogp/nascar XHR endpoints** (killed 2026-07-03): resolved in practice — all three ship full results via Pulselive JSON / Wikipedia / motorsport.com pipelines, no Playwright in any data path.
- **Migrate mdx-components to Paddock tokens** (killed 2026-07-03): premise invalid — the file carries no styling; the real residual (blog `prose-zinc` wrapper) folded into the 2026-07-03 blog batch.
