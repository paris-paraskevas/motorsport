# Reimagining — next-session handoff (written 2026-08-19, session 28/29 boundary)

## State
- main = **0.287.0** (Paper default + full-width + header doors, this PR). Sessions 28 shipped #679–#697: theme foundation, four-door shell, NavPanel v2 (wide), editorial home, orphan sweep, calendar (bars/chips), series hub (7a), series landing (4a), weekend report/preview split, driver profile, Learn, wire, telemetry, account, blog index, lockfile fix (nested `@serwist/turbopack/@swc/helpers` — npm10 ci green).
- **Prod UNFROZEN at 0.288.0 (2026-08-19 08:10Z).** Root cause was the Cloudflare Worker crossing the hard 10 MiB gzip script limit at 0.275.0; fixed by the 0.288.0 diet (dead MDX toolchain + server Sentry out — operator-approved). Margin is thin (~200 KiB, worker at 10,042/10,240) — the durable fix is moving the content bundle to static assets (~650 KiB, zero loss). Warm-live-data cron GREEN; every merge now auto-deploys again (~6 min).
- Design source: `C:\Users\ppara\Downloads\Reimagining (1)\design_handoff_paddock_ui_reimagining\` — README + `Home Dashboard.dc.html` (serve: `python -m http.server 8777` in that dir; screenshot panels via Playwright `[id="3a"]` etc.).

## Operator's ordered list (2026-08-19 notes, images = the mocks; execute in order, one PR each, merge immediately)
Done this session: ① width caps off + Paper default for everybody (SSR `data-theme="paper"`, no dark class, ThemeScript/Picker fallbacks → paper, Clerk paper palette, themeColor #f7f3e8) + header door links (Calendar/Learn/Account, `DoorLinks` in AppShell).
Done session 29 (2026-08-19): ② NavPanel v3 = 0.289.0 · ③ calendar = 0.290.0 · GSC soft-404 metadata guards = 0.291.0 (operator triage; June MDX posts stay dead; `/$`, redirects, canonicals need nothing) · 404 page Paper redesign = 0.292.0 · ④ session page 11d/12c/12d = 0.293.0 (incl. fold-in part 1: `[session]/loading.tsx` → in-page Suspense; NASCAR stage-points + WRC stage-ledger documented deferrals) · ⑤ weekend 3a/3b polish = 0.294.0 (fold-in complete: weekend + series skeletons gone; **prod-verified: all five GSC dead URLs now real 404s**, blog rail live) · ⑥ landing 10a = 0.295.0 (live This-weekend/Last-time-out hero, InstallApp, NavPanel header; deviations: no Clerk-bound bottom bar on the bare marketing layout, footer kept for legal links; orphans pending deletion approval: TickerBar/Hero/MarqueeEvent/SeriesMarquee/StatsBand/FeatureBlocks/PredictionGame/DisciplinesGrid/PerksCta/LandingMenu/BigCountdown/clean-title + WeekendHero) · ⑦ driver rail = 0.296.0 (venue sub-lines, podiums stat, Next-out countdown, mentions rail; fold-in FINAL — every GSC streamed route now true-404s) · ⑧ predictions 10c = 0.297.0 (Open-now cards + locks line, open calls, win-rate league rail, house-rules foot; "resets on the 1st" NOT rendered — leaderboard is all-time; signed-in body needs an operator eyeball on prod) · ⑨ copy passes = 0.298.0 (wire/Learn/account/telemetry/blog-post/hub-matrix; blog classification embed deferred to IDEAS — no such embed type exists yet; "Weekend report →" became "{series} coverage →" — posts carry no round). **THE ORDERED LIST IS COMPLETE — all nine jobs live.**
2. **NavPanel v3 = panel 2a/#32 EXACT**: left column becomes NOW (Home /app, Calendar /calendar, Latest results →/app, This weekend →/calendar — plain serif links, no subtitles) + READ (Blog, News wire, Writers /authors); ALSO foot gains right-aligned `SETTINGS →`; widen panel to ~min(1200px,100vw-3rem). File: components/NavPanel.tsx.
3. **Calendar**: toolbar center label must follow the view (month name / week range / day) with arrows already stepping by view; add TODAY button (top right); "APPLIED AS YOU TAP" note right of chips; "SUBSCRIBE TO THIS CALENDAR →" foot (webcal /api/calendar/[slug].ics has per-series only — all-series ics? check /api/calendar; else link the visible series' subscribe on /series/[slug]); ADD the 5a SEASON timeline as a 4th view (month sections, weekend blocks grouped by slug:round via roundByKey/roundNames, jump-to-month chips that scroll, badges: DOUBLE (2 rounds same series same weekend), FINALE (last round), SEPANG-style relocation chips via rounds.json rescheduleNote, per-row date-range chips when overlapping, BEHIND YOU foot). Files: components/calendar/CalendarToolbar.tsx, CalendarView.tsx, new SeasonView in calendar dir.
4. **Session page per 11d/12c/12d** (`[session]/page.tsx` render section): boxed session chips + "NO SPRINT AT THIS ROUND" note (F1 non-sprint weekends), table with mono heads POS NO DRIVER TEAM TIME GAP PTS, serif names, winner row raised, 6 rows default + `<details>` "N MORE CLASSIFIED · N RETIRED — SHOW ALL →" expander (retirements at foot with cause), foot "TIMING DATA VIA OPENF1" + "BACK TO THE WEEKEND →"; multi-class per 12c: OVERALL/class chips, per-class blocks, crew mono under team serif, car #, "GAPS BECOME LAPS ONCE A CLASS IS LAPPED · A CREW OF THREE SHARES ONE RESULT" foot, "ALL N CARS →". **Fold-in (0.291.0 decision):** drop this segment's `loading.tsx` and stream heavy sections via in-page `<Suspense>` BELOW the notFound() guards — a flushed skeleton locks the status at 200, so dead session slugs can't true-404 while it exists (Next docs, loading.md §Status Codes).
5. **Weekend polish per 3a/3b (#28-30)**: report — full-field `<details>` expander + "DEFAULT IS THE POINTS-SCORING POSITIONS…" footnote + FROM THE BLOG rail (publishedPostsForSeries); preview — serif masthead like report + first-session countdown card + ADD TO CALENDAR + GOING IN 3-row standings factbox + venue rail w/ map; footer line (technical file · predictions close · news · next round →). **Fold-in (0.291.0 decision):** convert `weekend/[round]/loading.tsx` AND `series/[slug]/loading.tsx` (ancestor boundaries capture children) to in-page `<Suspense>` below the guards, so out-of-range rounds return real 404s — the GSC soft-404 four.
6. **Landing = 10a/#18 EXACT** (`app/(marketing)/page.tsx` rebuild): serif hero "Every session, every result, in your own time zone", paragraph, SEE WHAT IS ON NOW (solid ink → /app) + INSTALL AS AN APP buttons, right ink-border panel THIS WEEKEND (real, reuse calendar ThisWeekend logic) + LAST TIME OUT (fetchLatestPodium winner + margin), three numbered promises, foot "AN ACCOUNT ADDS … CREATE ONE, FREE →". Old carousel components (Hero/TickerBar/etc.) orphan afterward — deletion PR after approval.
7. **Driver page rail (#13)**: two-col — every-round table left (add venue sub-line from RaceResult.circuit), rail right: NEXT OUT (next weekend + countdown), IN THE NEWS (mentions — exists), COMPARE WITH A TEAM-MATE → + TEAM PAGE →; stat line add podiums (derive count position<=3 in profile-stats). **Fold-in (0.291.0 decision):** `drivers/[slug]/loading.tsx` → in-page `<Suspense>` below the guard, same soft-404 reason.
8. **Predictions /social per 10c/#20**: header + balance mono 36px, OPEN NOW panel (markets from existing betting libs) + LOCKS line, market cards (5), YOUR OPEN CALLS, YOUR LEAGUE rail ranked on win rate, HOUSE RULES foot (constants.ts numbers). Read app/(app)/social/* first — operator-fresh code, restyle not rewrite.
9. **Copy/layout passes**: wire header → serif "The wire" + standfirst + "TEN PER SERIES, DEDUPED…" foot; Learn header → "Motorsport, explained" + placeholder “what is DRS”, “how do WEC points work” + "75 ANSWERS, ALL SOURCED" label + topics 2-col with /ROUTE → labels; account (#15): SIGN OUT (Clerk SignOutButton) + EXPORT YOUR DATA (contact modal) foot — do NOT resurrect Customise home (deleted by cutover decision); telemetry cards copy → "How pole was taken"/"How the race was won"/"Compare two drivers" + 2-col round list; blog post page (#19): serif 44px reading column at 760px, eyebrow series·round·read-time, byline band + WEEKEND REPORT →, pull-quote style, embedded live classification (check lib/blog-embeds for an existing result embed); **#10 matrix**: "One layout, fifteen sports" table (handoff README §3 data verbatim) as a section at the foot of /series hub.

## Round 2 — operator's annotated screenshots (2026-08-19 evening; 19 images) — **COMPLETE, all ten jobs live (0.301.0–0.309.0; ⑩ folded into ②)**

Leftover noticed en route (queued in IDEAS, not part of the batch): the app error boundary ("Something broke" gradient card) is pre-Paper; `listThreads` has no fail-soft (local-DB-down dev renders the boundary instead of the page).

1. ✅ 0.301.0 — **FIX `/contact` 404 (live bug, first).** The Account "Export your data" row (0.298.0), the footer Contact link and the 404-page annotation all point at `/contact` — it 404s on prod. Find the route's fate (removed? group move?), restore it as a Paper-register page (the old form posted to `/api/contact`, KV 12-month retention). Verify: curl 200 + form renders + the export row lands on it.
2. ✅ 0.302.0 (41 strings / 27 files; ⑩ mechanic folded in) — **CTA palette sweep — the amber pills are pre-Paper.** Grep `brand-fill` (and amber/rounded pill classes) app-wide and disposition every hit: weekend **Bets tab** sign-in CTAs ("Sign in to back the winner" etc. — MarketBetCard), **/write-for-us** "Sign in to apply", **/settings** "Sign in". Replace with the ink/bordered mono button language (the landing's `bg-text text-bg` + `border-border-strong` pair).
3. ✅ 0.303.0 — **Account trio Paper restyle** — `/settings`, `/settings/series`, `/settings/notifications` still read as the OLD site (operator: "width is off, styling resembles old site"; three separate screenshots). Serif mastheads (kill the `ACCOUNT.` / `CHAMPIONSHIPS.` / `NOTIFICATIONS.` display-caps register), reimagined page width (not the boxed centre column), ink Sign-in button, and the floating dashed **CHOOSE SERIES** chip integrated into the You-follow row (operator: "box looks out of place").
4. ✅ 0.304.0 — **Header doors + panel behaviour** — header becomes **Calendar · Learn · Series** ("Instead of account have series here"; Account stays reachable via the panel's Settings → and the avatar). NavPanel: anchor the sheet **under the burger/field** (drop the viewport-centred fixed positioning) and open it on **burger hover** (desktop) as well as click/focus.
5. ✅ 0.305.0 — **Home lead two-up** — "What it changed" and "What's next" share one band: the standings read (headline + bars) left, the next-three-weekends list right (operator's arrows move What's-next up beside the championship table).
6. ✅ 0.306.0 — **Calendar fits the screen** — month view must fit a large display without page scroll ("my screen is massive yet the calendar still doesnt fit"): cap day-cell height with a "+N more" overflow, tighten row density, reclaim the toolbar rows. Move **TODAY** from the top-right corner to sit beside the Month/Week/Day/Season switcher.
7. ✅ 0.307.0 — **Weekend preview layout v2** — widen the schedule block to the annotated width; the **circuit map leaves the rail and renders LARGE in the main column beside the schedule** (two annotations agree); bring the **News module up into the main column** where the standings-at-this-round band sits. The rail keeps First session · Add to calendar · Going in · venue text · Where to watch.
8. ✅ 0.308.0 (cells already led to distinct pages — probed 200 across series; added the missing → affordance; Calendar rail → /calendar?s=) — **Series-page routing** — every Learn-about cell must lead to a **distinct page, for all 15 series** (Champions → `/series/[slug]/champions`, Drivers → `/series/[slug]/drivers`, About/History/Rules/Points/What's-new → their information pages); the tab-strip **CALENDAR** entry links to `/calendar?s={slug}` (the 0.290.0 deep-link filter) instead of the in-page tab.
9. ✅ 0.309.0 (dev visual blocked by local-DB — sentry verifies prod markers) — **Threads Paper restyle** — `/social/threads` (+ `/threads/[id]`) to the Paper register (serif masthead, bordered rows, mono meta) — "resembles old site pages".
10. ✅ folded into 0.302.0 (ink square + border, send button inked) — **Mechanic widget restyle** — the floating yellow bottom-right bubble is "far off the styling of this site now" — restyle to Paper (ink/oxblood, mono label); find its component by the bubble's icon/role.

## Round 3 — 2026-08-20 (feedback board + live messages + 5 annotated screenshots)

**Live queue (operator present, asks explicit):**
1. ✅ 0.310.0 — **Season-complete clarity** (feedback board "Formula E is done, that needs to be clear" + follow-up "season complete first thing, champion outranks race winner"): home lead band leads SEASON COMPLETE → champion h1 → race winner sub-line; series landing masthead/final-table/champion callout.
2. ✅ 0.311.0 — **Distinct lines site-wide on Paper** (`--border` #d6cebb→#c2b493, `--border-strong` #a99c80→#91825e) + calendar nav bar ink rules with the arrow boxes cut by ink verticals (operator annotation).
3. ✅ 0.312.0 — **Calendar series-filter box**: bordered panel, all 15 series as checkbox chips (no "+N more"), SELECT ALL / CLEAR, checkbox semantics (untick-from-all excludes just it; empty = transient none, never persisted). Also 0.311.1 en route: home podium names its race in champion mode.
4. ✅ 0.313.0 — **Mobile pass**: month view becomes an agenda below md (day headers, bold deciders, collapsed runs; the dot grid is gone); THIS WEEKEND cards 2-up on phones; filter box collapses to its summary line with EDIT below md; series-landing status column hides below sm (long race names stop truncating). Sweep: weekend preview/report, session, series hub, driver, social, information, landing, blog, news probed at 375 — zero horizontal overflow, session table already adapts; nothing left to ledger.

**The old series tab pages (5 screenshots — `/series/f1/*` still wear the pre-Paper register: display-caps masthead, LEARN ABOUT grid, CALENDAR/STANDINGS/RESULTS/ROUNDS tab strip):**
5. ✅ 0.314.0 + 0.315.0 — **Sub-page shell + Champions rebuild**: `SeriesPageView` rewritten in Paper (breadcrumb → serif subject masthead → body → mono cross-link foot; old display-caps masthead, Learn grid, tab strip and dead calendar branches gone — all five pages changed at once); Champions got radio-driven **Drivers'/Team champions tabs** (zero JS, all panels in the HTML) with open decade sections replacing the accordions.
6. ✅ 0.316.0 — **Drivers rebuild**: lineup joined with the live drivers' championship (per-row P·pts·wins, per-team combined pts · best P, teams ranked by combined points, Full standings → link; code→slug→unique-last-name join keys; fail-soft plain lineup for multi-class series). New `fetchFullDriverStandings` in `lib/standings/brief.ts`.
7. ✅ 0.314.0 (scope note) — **Results / Standings / Rounds**: new Paper shells + **Standings & Results cells added to the landing's reference row** (the operator's "more easily accessible" sketch). Their BODIES (results list, standings tables + trend, circuit grid) kept — token-consistent and data-dense; audited under the new shells (results body is fail-soft-empty on local dev only — feed is prod-side). Deeper body rework, if wanted, is a new ask.

## Ritual per PR (hard-won)
`git checkout -b <branch> main` FIRST (main-edit slip happened once) → edits → `rm -rf .next/dev` (stale dev-types fail next build — landmine 9) → tsc → lint (grep "problems") → vitest (1116; a lone fail alongside a running dev server = the documented flake, rerun clean) → `npm run build` and CHECK exit before anything else → dev + Playwright verify (kill dev BY PORT PID, never image) → trio (package.json + CHANGELOG eng + RELEASES public, next = 0.288.0) → commit (no attribution) → push → `gh pr create` (body: what/why/verified + "operator-authorized run") → `gh pr merge --squash --delete-branch` → poll prod only if the pipeline is fixed.
Union-resolve trio conflicts; python heredocs for multi-edit scripts (quote-safe via Write to scratchpad); screenshots to scratchpad, never repo root.
