# Reimagining — next-session handoff (written 2026-08-19, session 28/29 boundary)

## State
- main = **0.287.0** (Paper default + full-width + header doors, this PR). Sessions 28 shipped #679–#697: theme foundation, four-door shell, NavPanel v2 (wide), editorial home, orphan sweep, calendar (bars/chips), series hub (7a), series landing (4a), weekend report/preview split, driver profile, Learn, wire, telemetry, account, blog index, lockfile fix (nested `@serwist/turbopack/@swc/helpers` — npm10 ci green).
- **Prod UNFROZEN at 0.288.0 (2026-08-19 08:10Z).** Root cause was the Cloudflare Worker crossing the hard 10 MiB gzip script limit at 0.275.0; fixed by the 0.288.0 diet (dead MDX toolchain + server Sentry out — operator-approved). Margin is thin (~200 KiB, worker at 10,042/10,240) — the durable fix is moving the content bundle to static assets (~650 KiB, zero loss). Warm-live-data cron GREEN; every merge now auto-deploys again (~6 min).
- Design source: `C:\Users\ppara\Downloads\Reimagining (1)\design_handoff_paddock_ui_reimagining\` — README + `Home Dashboard.dc.html` (serve: `python -m http.server 8777` in that dir; screenshot panels via Playwright `[id="3a"]` etc.).

## Operator's ordered list (2026-08-19 notes, images = the mocks; execute in order, one PR each, merge immediately)
Done this session: ① width caps off + Paper default for everybody (SSR `data-theme="paper"`, no dark class, ThemeScript/Picker fallbacks → paper, Clerk paper palette, themeColor #f7f3e8) + header door links (Calendar/Learn/Account, `DoorLinks` in AppShell).
Done session 29 (2026-08-19): ② NavPanel v3 = 0.289.0 · ③ calendar = 0.290.0 · GSC soft-404 metadata guards = 0.291.0 (operator triage; June MDX posts stay dead; `/$`, redirects, canonicals need nothing) · 404 page Paper redesign = 0.292.0 · ④ session page 11d/12c/12d = 0.293.0 (incl. fold-in part 1: `[session]/loading.tsx` → in-page Suspense; NASCAR stage-points + WRC stage-ledger documented deferrals) · ⑤ weekend 3a/3b polish = 0.294.0 (fold-in complete: weekend + series skeletons gone; **prod-verified: all five GSC dead URLs now real 404s**, blog rail live) · ⑥ landing 10a = 0.295.0 (live This-weekend/Last-time-out hero, InstallApp, NavPanel header; deviations: no Clerk-bound bottom bar on the bare marketing layout, footer kept for legal links; orphans pending deletion approval: TickerBar/Hero/MarqueeEvent/SeriesMarquee/StatsBand/FeatureBlocks/PredictionGame/DisciplinesGrid/PerksCta/LandingMenu/BigCountdown/clean-title + WeekendHero).
2. **NavPanel v3 = panel 2a/#32 EXACT**: left column becomes NOW (Home /app, Calendar /calendar, Latest results →/app, This weekend →/calendar — plain serif links, no subtitles) + READ (Blog, News wire, Writers /authors); ALSO foot gains right-aligned `SETTINGS →`; widen panel to ~min(1200px,100vw-3rem). File: components/NavPanel.tsx.
3. **Calendar**: toolbar center label must follow the view (month name / week range / day) with arrows already stepping by view; add TODAY button (top right); "APPLIED AS YOU TAP" note right of chips; "SUBSCRIBE TO THIS CALENDAR →" foot (webcal /api/calendar/[slug].ics has per-series only — all-series ics? check /api/calendar; else link the visible series' subscribe on /series/[slug]); ADD the 5a SEASON timeline as a 4th view (month sections, weekend blocks grouped by slug:round via roundByKey/roundNames, jump-to-month chips that scroll, badges: DOUBLE (2 rounds same series same weekend), FINALE (last round), SEPANG-style relocation chips via rounds.json rescheduleNote, per-row date-range chips when overlapping, BEHIND YOU foot). Files: components/calendar/CalendarToolbar.tsx, CalendarView.tsx, new SeasonView in calendar dir.
4. **Session page per 11d/12c/12d** (`[session]/page.tsx` render section): boxed session chips + "NO SPRINT AT THIS ROUND" note (F1 non-sprint weekends), table with mono heads POS NO DRIVER TEAM TIME GAP PTS, serif names, winner row raised, 6 rows default + `<details>` "N MORE CLASSIFIED · N RETIRED — SHOW ALL →" expander (retirements at foot with cause), foot "TIMING DATA VIA OPENF1" + "BACK TO THE WEEKEND →"; multi-class per 12c: OVERALL/class chips, per-class blocks, crew mono under team serif, car #, "GAPS BECOME LAPS ONCE A CLASS IS LAPPED · A CREW OF THREE SHARES ONE RESULT" foot, "ALL N CARS →". **Fold-in (0.291.0 decision):** drop this segment's `loading.tsx` and stream heavy sections via in-page `<Suspense>` BELOW the notFound() guards — a flushed skeleton locks the status at 200, so dead session slugs can't true-404 while it exists (Next docs, loading.md §Status Codes).
5. **Weekend polish per 3a/3b (#28-30)**: report — full-field `<details>` expander + "DEFAULT IS THE POINTS-SCORING POSITIONS…" footnote + FROM THE BLOG rail (publishedPostsForSeries); preview — serif masthead like report + first-session countdown card + ADD TO CALENDAR + GOING IN 3-row standings factbox + venue rail w/ map; footer line (technical file · predictions close · news · next round →). **Fold-in (0.291.0 decision):** convert `weekend/[round]/loading.tsx` AND `series/[slug]/loading.tsx` (ancestor boundaries capture children) to in-page `<Suspense>` below the guards, so out-of-range rounds return real 404s — the GSC soft-404 four.
6. **Landing = 10a/#18 EXACT** (`app/(marketing)/page.tsx` rebuild): serif hero "Every session, every result, in your own time zone", paragraph, SEE WHAT IS ON NOW (solid ink → /app) + INSTALL AS AN APP buttons, right ink-border panel THIS WEEKEND (real, reuse calendar ThisWeekend logic) + LAST TIME OUT (fetchLatestPodium winner + margin), three numbered promises, foot "AN ACCOUNT ADDS … CREATE ONE, FREE →". Old carousel components (Hero/TickerBar/etc.) orphan afterward — deletion PR after approval.
7. **Driver page rail (#13)**: two-col — every-round table left (add venue sub-line from RaceResult.circuit), rail right: NEXT OUT (next weekend + countdown), IN THE NEWS (mentions — exists), COMPARE WITH A TEAM-MATE → + TEAM PAGE →; stat line add podiums (derive count position<=3 in profile-stats). **Fold-in (0.291.0 decision):** `drivers/[slug]/loading.tsx` → in-page `<Suspense>` below the guard, same soft-404 reason.
8. **Predictions /social per 10c/#20**: header + balance mono 36px, OPEN NOW panel (markets from existing betting libs) + LOCKS line, market cards (5), YOUR OPEN CALLS, YOUR LEAGUE rail ranked on win rate, HOUSE RULES foot (constants.ts numbers). Read app/(app)/social/* first — operator-fresh code, restyle not rewrite.
9. **Copy/layout passes**: wire header → serif "The wire" + standfirst + "TEN PER SERIES, DEDUPED…" foot; Learn header → "Motorsport, explained" + placeholder “what is DRS”, “how do WEC points work” + "75 ANSWERS, ALL SOURCED" label + topics 2-col with /ROUTE → labels; account (#15): SIGN OUT (Clerk SignOutButton) + EXPORT YOUR DATA (contact modal) foot — do NOT resurrect Customise home (deleted by cutover decision); telemetry cards copy → "How pole was taken"/"How the race was won"/"Compare two drivers" + 2-col round list; blog post page (#19): serif 44px reading column at 760px, eyebrow series·round·read-time, byline band + WEEKEND REPORT →, pull-quote style, embedded live classification (check lib/blog-embeds for an existing result embed); **#10 matrix**: "One layout, fifteen sports" table (handoff README §3 data verbatim) as a section at the foot of /series hub.

## Ritual per PR (hard-won)
`git checkout -b <branch> main` FIRST (main-edit slip happened once) → edits → `rm -rf .next/dev` (stale dev-types fail next build — landmine 9) → tsc → lint (grep "problems") → vitest (1116; a lone fail alongside a running dev server = the documented flake, rerun clean) → `npm run build` and CHECK exit before anything else → dev + Playwright verify (kill dev BY PORT PID, never image) → trio (package.json + CHANGELOG eng + RELEASES public, next = 0.288.0) → commit (no attribution) → push → `gh pr create` (body: what/why/verified + "operator-authorized run") → `gh pr merge --squash --delete-branch` → poll prod only if the pipeline is fixed.
Union-resolve trio conflicts; python heredocs for multi-edit scripts (quote-safe via Write to scratchpad); screenshots to scratchpad, never repo root.
